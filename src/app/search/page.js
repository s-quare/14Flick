import SearchClient from "@/components/SearchClient";

export const metadata = {
  title: "Search Results",
  description:
    "Search for your favorite movies, TV shows, and actors on 14flick.",
  robots: "noindex, follow",
};

function SearchPage() {
  <div>
    <SearchClient />
  </div>;
}

export default SearchPage;
